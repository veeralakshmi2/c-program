n=int(input())
arr2=[]
arr=list(map(int,input().split()))
i=0
for i in arr:
    cnt=0
    j=0
    for j in arr:
        if arr[i]==arr[j]:
            cnt=cnt+1
    if cnt==1:
        arr2.append(arr[i])
print(arr2)
